#!/usr/bin/env bash
set -Eeuo pipefail

source "$HOME/.config/theme-system/lib/core.sh"
source "$HOME/.config/theme-system/lib/waybar.sh"

mode="$1"

if [ -z "$mode" ]; then
    echo "Usage: waybar-mode <mode>"
    exit 1
fi

set_waybar_mode "$mode" || exit 1

reload_services
